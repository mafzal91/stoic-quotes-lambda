import { ConnectOptions, Introspector } from '../../introspector';
import { DatabaseMetadata } from '../../metadata';
import { PostgresDB } from './postgres-db';
export declare class PostgresIntrospector extends Introspector<PostgresDB> {
    #private;
    introspect(options: ConnectOptions): Promise<DatabaseMetadata>;
}
