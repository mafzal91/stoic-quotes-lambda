import { PostgresDialect as KyselyPostgresDialect, TableMetadata } from 'kysely';
import { Dialect, DriverInstantiateOptions } from '../../dialect';
import { PostgresAdapter } from './postgres-adapter';
export declare class PostgresDialect extends Dialect {
    readonly adapter: PostgresAdapter;
    createKyselyDialect(options: DriverInstantiateOptions): Promise<KyselyPostgresDialect>;
    getExportedTableName(table: TableMetadata, camelCase: boolean): string;
    getSymbolName(table: TableMetadata): string;
}
