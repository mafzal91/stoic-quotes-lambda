import { AdapterDefinitions, AdapterTypes } from './adapter';
export declare type EnumMap = {
    [K in string]?: string[];
};
export declare class EnumCollection {
    readonly enums: EnumMap;
    add(key: string, value: string): void;
    get(key: string): string[] | null;
    getDefinitions(): AdapterDefinitions;
    getTypes(): AdapterTypes;
    set(key: string, values: string[]): void;
}
