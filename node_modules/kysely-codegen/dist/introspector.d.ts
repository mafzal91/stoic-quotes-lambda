import { Kysely, TableMetadata } from 'kysely';
import { Dialect } from './dialect';
export declare type CreateKyselyOptions = IntrospectOptions & {
    ssl: boolean;
};
export declare type IntrospectOptions = {
    connectionString: string;
    dialect: Dialect;
    excludePattern?: string;
    includePattern?: string;
};
/**
 * Uses the Kysely introspector to gather table metadata from a database connection.
 */
export declare class Introspector {
    createKysely(options: CreateKyselyOptions): Promise<Kysely<any>>;
    introspect(options: IntrospectOptions): Promise<TableMetadata[]>;
}
